version: '3.4'

services:
  despesas-pessoais-frontend-react-proto:
    image: alexfariakof/despesas-pessoais-frontend-react-prototipo-img
    build:
      context: .
      dockerfile: ./DockerFile-Production
    environment:
      NODE_ENV: production
    ports:
      - 80:80